spring:
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

  devtools:
    restart:
      enabled: true
      poll-interval: 2s
      quiet-period: 1s

    livereload:
      enabled: true
